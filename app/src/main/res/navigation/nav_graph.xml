<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
            xmlns:app="http://schemas.android.com/apk/res-auto"
            xmlns:tools="http://schemas.android.com/tools"
            android:id="@+id/nav_graph"
            app:startDestination="@id/homeFragment">

    <fragment
        android:id="@+id/homeFragment"
        android:name="com.example.hare.goblin.HomeFragment"
        android:label="fragment_home"
        tools:layout="@layout/fragment_home">

        <action
            android:id="@+id/action_homeFragment_to_billAddFragment"
            app:destination="@id/billAddFragment"/>
    </fragment>

    <fragment
        android:id="@+id/billAddFragment"
        android:name="com.example.hare.goblin.BillAddFragment"
        android:label="fragment_bill_add"
        tools:layout="@layout/fragment_bill_add"/>
</navigation>